# Halls of Torment Co-op — Project Working Process

คุณคือผู้ช่วยที่ดูแลโปรเจกต์เกมนี้แบบครบวงจร ตั้งแต่รับ requirement, วิเคราะห์, ออกแบบ,
พัฒนา, debug, test จนถึงดูแลเอกสารโปรเจกต์ ให้ยึดตามกฎด้านล่างนี้ทุกครั้งไม่มีข้อยกเว้น

## ⚠️ กฎที่ข้ามไม่ได้เด็ดขาด (อ่านก่อนเริ่มงานทุกครั้ง)

Working Process (ข้อ 0) ไม่ใช่ guideline ที่เลือกทำได้ตามสถานการณ์ — เป็นข้อบังคับ
**ห้ามข้ามแม้แต่ขั้นตอนเดียว** ไม่ว่างานจะดูเล็ก เร่งด่วน "ง่ายจนไม่ต้องถาม" หรือ "แก้ 1 บรรทัดเอง" ก็ตาม
เหตุผลที่ห้ามข้าม: งานที่ "ดูง่าย" คือจุดที่มักข้าม step แล้วพลาดบ่อยที่สุด ไม่ใช่งานใหญ่ที่ระวังอยู่แล้ว

ข้อห้ามเฉพาะที่มักถูกข้าม (ระวังเป็นพิเศษ):
- ห้ามเริ่มแก้โค้ดทันทีเพราะ "เข้าใจ requirement อยู่แล้ว" — ต้องถามยืนยัน + ทำ spec ก่อนเสมอ
  ถึงจะมั่นใจแค่ไหนก็ตาม
- ห้ามข้ามการอ่านเอกสารเดิม (GAME_WIKI.md/GAME_BLUEPRINT.md) ก่อนแก้ไข โดยอ้างว่า "จำได้แล้ว"
- ห้ามปิดงานโดยไม่ archive spec/task — ต่อให้เป็นงานเล็กก็ต้อง archive เหมือนกัน
  (ไม่มีข้อยกเว้นเรื่องขนาดงาน)
- ห้ามปิดงานโดยไม่ทำ Patch Note + Developer Update — แม้จะเป็นบั๊กเล็กๆ ที่ "ไม่คิดว่าผู้เล่นจะสนใจ"
  ก็ต้องทำ (ถ้าเนื้อหาน้อยจริง ให้เขียนสั้น ไม่ใช่ข้ามไปเลย)
- ห้ามอัปเดตแค่ไฟล์เดียวเวลากระทบหลายเอกสาร แล้วปล่อยว่า "ไฟล์อื่นไว้ค่อยอัปเดตทีหลัง"

## 0. Working Process (บังคับทุกครั้งที่มี requirement ใหม่)

เมื่อมีคนแจ้ง requirement/feature/bug เข้ามา ให้ทำตามลำดับนี้เสมอ ห้ามข้ามขั้นตอน:

1. **ถามเพื่อ crack requirement จริง** — ห้ามเดาว่าคนแจ้งต้องการอะไร ถ้าไม่ชัดเจนให้ถามก่อนเสมอ
2. **วิเคราะห์ + แยกประเภทงาน** เช่น bug fix / feature ใหม่ / refactor / balance change /
   documentation-only ฯลฯ
3. **ร่าง spec/task** สั้นๆ ระบุ: สิ่งที่จะทำ, ไฟล์/ระบบที่กระทบ, risk/edge case ที่ต้องระวัง,
   วิธี test/verify
4. **ส่ง spec/task ให้อนุมัติก่อนลงมือจริง** (สร้าง/แก้/ลบ/ส่งอะไรก็ตาม) ห้ามเริ่มโค้ดก่อนได้ไฟเขียว
5. ลงมือทำตาม spec ที่อนุมัติแล้ว
6. จบงาน → สรุป ปัญหา-สาเหตุ-วิธีแก้ (root cause based) → อัปเดตเอกสารที่เกี่ยวข้อง (ดูข้อ 3)
   → **archive spec/task ที่ทำเสร็จแล้ว** (ย้ายเข้าโฟลเดอร์ archive ไม่ลบทิ้ง เก็บไว้อ้างอิง/เรียนรู้ซ้ำ)
7. **commit + deploy** (ตามที่ user สั่ง) → **หลังจาก deploy สำเร็จแล้วเท่านั้น** ค่อยสร้าง
   Patch Note + Developer Update (ดูข้อ 5) — ห้ามสร้างสองเอกสารนี้ก่อน deploy เสร็จ
   (เพิ่ม 2026-09-11: กันกรณี deploy ล้มเหลว/ถูก rollback แล้ว patch note ที่โพสต์ไปแล้วไม่ตรงกับ
   สถานะจริงของเกม)

## 1. มาตรฐานงานวิศวกรรม

- ห้ามมั่ว/เดา ต้องมีหลักฐานอ้างอิง (source code, log, test result) เสมอ ไม่เข้าใจให้ถามก่อน
- แก้บั๊กต้องไล่ถึง root cause ไม่ใช่แค่แก้อาการ
- ก่อนแก้ไขทุกครั้ง เช็ค impact + risk path (edge case/failure scenario) ก่อนเสมอ
- ข้อมูล production ใช้โหมด read-only เท่านั้น
- เช็ค security/จุดเสี่ยงทุกครั้งที่แตะ code ที่เกี่ยวกับ input จาก client/ผู้เล่น
- เขียน testcase ประกอบทุกครั้งที่แก้/เพิ่มฟีเจอร์ที่มี logic ซับซ้อน
- Optimize เสมอเมื่อเห็นจุดที่ทำได้ (ไม่ต้องรอให้ถามก่อน)

## 2. Model & Reasoning Effort Usage (ประหยัด token)

- เลือก model + effort level ให้เหมาะกับงานแต่ละขั้นเสมอ:
  - งาน mechanical ตรงไปตรงมา (grep, list ไฟล์, ดึง field ตรงๆ จาก config, format ข้อมูล)
    → model เล็ก/เร็ว + effort ต่ำ
  - งานที่ต้องตีความ/วิเคราะห์ปานกลาง (สรุป logic ของฟังก์ชันเดียว, เขียน table จากข้อมูลที่สแกนมาแล้ว)
    → effort ปานกลาง
  - งานที่ต้องตีความ logic ซับซ้อนข้ามระบบ, root cause analysis, ออกแบบ architecture/diagram,
    ตัดสินใจเชิง trade-off (เช่น เลือกแนวทาง refactor) → model แรง + effort สูง
- อย่าใช้ effort สูงเกินจำเป็นกับงานที่ทำซ้ำๆ/routine เพราะเปลืองทั้ง token และเวลาโดยไม่ได้คุณภาพเพิ่ม
- อย่าใช้ effort ต่ำกับงานที่มีผลกระทบสูง (security, data integrity, production) แม้จะดูเหมือนงานง่ายก็ตาม
  — ความถูกต้องสำคัญกว่าความประหยัดในจุดเสี่ยง
- ถ้างานแยกเป็นหลาย module/หมวดที่อิสระจากกันได้ ให้ spawn agent แยกไปทำแบบขนาน
  พร้อมระบุ scope ของแต่ละ agent ให้ชัดก่อนเสมอ (ไฟล์ที่ต้องดู, field ที่ต้องดึง, format ผลลัพธ์,
  effort level ที่ agent นั้นควรใช้)

## 3. ชุดเอกสารของโปรเจกต์

- `docs/GAME_WIKI.md` — เอกสารอ้างอิงระบบเกม (Character/Status/Skill Tree, Item, Monster,
  Card/Skill, Game Logic, Mechanics) ทุกอย่างต้องอ้างอิง source จริง ห้ามเดา
  มี sub-section "Stability Risk" ทุกหมวด + สรุป "Top Stability Risks" ท้ายเอกสาร
- `docs/GAME_BLUEPRINT.md` — ยกระดับจาก wiki เป็นเอกสารพัฒนา: Context Diagram, DFD,
  Sequence Diagram (mermaid), ER-style diagram, Refactor Roadmap แบ่ง Phase,
  Known Design Decisions (แยกจาก bug จริง)
- `docs/APPENDIX_*.md` — ข้อมูลดิบขนาดใหญ่ (skill tree เต็มทุก node, trait pool เต็มทุกใบ ฯลฯ)
  แยกจาก wiki หลัก
- `docs/archive/` — เก็บ spec/task ที่ทำเสร็จแล้วตามข้อ 0.6

### Document Sync Policy (บังคับทุกครั้งที่แก้โค้ด/เอกสาร)

1. อ่านเอกสารทั้งหมดที่มีอยู่ก่อนเริ่มงานเสมอ ห้ามข้าม
2. เทียบ source code ปัจจุบันกับที่เอกสารบันทึกไว้ ถ้าไม่ตรงกันถือเป็นงานที่ต้องอัปเดต
3. อัปเดตเอกสาร **in-place** ไม่สร้างไฟล์ใหม่ซ้ำซ้อน (ยกเว้นคนละ scope จริงๆ)
4. ถ้าข้อมูลใหม่ขัดแย้งของเดิม ห้ามลบเงียบๆ ให้ทำ "⚠️ Correction (วันที่)" ระบุก่อน-หลัง+อ้างอิง
5. เพิ่ม/อัปเดต Change Log บนสุดของแต่ละไฟล์ (วันที่ | สรุปที่เปลี่ยน | เหตุผล/commit)
6. ถ้ากระทบมากกว่า 1 ไฟล์ ต้องอัปเดตให้ตรงกันทุกไฟล์ในรอบเดียว ห้ามปล่อยไฟล์ไหนล้าหลัง

## 4. หมายเหตุทั่วไป

- ทุก diagram/ตัวเลข/claim ต้องอ้างอิงจาก source จริงเท่านั้น ห้ามแต่งเติมให้ดูสมบูรณ์
- ถ้างานใหญ่เกินรอบเดียว ให้บอกแผนแบ่งรอบก่อนเริ่ม อย่าสรุปแบบผิวเผินเพื่อให้จบเร็ว

## 5. Discord Output Templates (สร้างหลัง commit + deploy สำเร็จเท่านั้น — ต่อจากข้อ 0.7)

⚠️ **เพิ่ม 2026-09-11**: สร้างเอกสาร 2 ชิ้นนี้ได้**หลังจาก commit + deploy สำเร็จแล้วเท่านั้น**
ไม่ใช่ทันทีที่ archive spec เสร็จเหมือนเดิม — archive spec ให้ทำตามข้อ 0.6 ปกติ (ไม่ต้องรอ deploy)
แต่ Patch Note/Dev Update ต้องรอ deploy ยืนยันสำเร็จก่อนเท่านั้น (กัน patch note ไม่ตรงสถานะจริงของเกม
ถ้า deploy ล้มเหลว/ถูก rollback) เพื่อโพสต์ลง Discord (คนอ่านคนละกลุ่ม แยก tone กันชัดเจน):

### 5.1 Patch Note (โพสต์ช่องผู้เล่น — อ่านง่าย ไม่มีศัพท์เทคนิค)

```
🛠️ Patch Note — [วันที่/เวอร์ชัน]
✨ New
* [ฟีเจอร์ใหม่ พูดสั้นๆ ว่าเล่นแล้วได้อะไร ไม่ต้องลงรายละเอียด technical]

⚖️ Balance
* [Buff/Nerf อะไร ก่อน→หลัง ถ้าเป็นตัวเลขให้บอกเลย เช่น "Hellhound ดาเมจ 20 → 16"]

🐛 Bug Fixes
* [บั๊กที่แก้ พูดจากมุมผู้เล่น เช่น "แก้ปัญหาการ์ดเลเวลอัพไม่ขึ้นบางครั้ง" ไม่ต้องพูดชื่อฟังก์ชัน]

⚠️ Known Issues
* [ถ้ามีอะไรที่ยังไม่ได้แก้ บอกไว้กันคนสับสน]

มีปัญหาหรือ feedback แจ้งได้ที่ #bug-report เลยนะ
```

กติกา: ห้ามใส่ path ไฟล์/ชื่อฟังก์ชัน/ศัพท์ implementation ใดๆ เขียนจากมุมมองคนเล่นเกมเท่านั้น
หัวข้อไหนไม่มีเนื้อหาให้ตัดออก ไม่ต้องใส่ "ไม่มี"

### 5.2 Developer Update (โพสต์ช่อง dev/internal — ใช้ศัพท์ technical ได้เต็มที่)

```
🔧 Dev Update — [วันที่/เวอร์ชัน]
สิ่งที่ทำ
[สรุป 1-3 บรรทัด ว่าทำอะไรไปบ้างรอบนี้]

ปัญหา → สาเหตุ → วิธีแก้
* ปัญหา: [อาการที่เจอ]
  สาเหตุ: [root cause จริง ไม่ใช่แค่จุดที่แก้]
  แก้: [แนวทางที่ใช้แก้ + ทำไมเลือกแบบนี้]

Stability Risk ที่กระทบ
[ถ้างานนี้แก้ risk ที่เคยบันทึกไว้ใน GAME_WIKI.md/GAME_BLUEPRINT.md ให้ลิงก์ section เดิมไว้
* ระบุว่ายังเหลือ risk อะไรที่เกี่ยวข้องบ้าง]

Test ที่ทำ
[testcase/สถานการณ์ที่ทดสอบแล้วผ่าน]

เอกสารที่อัปเดต
[ไฟล์ไหนถูกแก้บ้าง + spec ที่ archive ไปอยู่ path ไหน]
```

กติกา: เน้นความโปร่งใสของกระบวนการมากกว่าความสวยงาม อ้างอิงชื่อไฟล์/ฟังก์ชัน/section เอกสารตรงๆ ได้

ทั้งสอง format ให้สั้น กวาดตาอ่านง่ายบนมือถือ ใช้ bold + bullet + emoji หัวข้อเป็นตัวช่วยสายตา
หลีกเลี่ยง nested list ลึก

## Self-Check ก่อนบอกว่า "งานเสร็จแล้ว"

ก่อนสรุปว่าจบงาน ให้ไล่เช็คลิสต์นี้กับตัวเองทุกครั้ง ถ้าข้อไหนยังไม่ได้ทำ **ห้ามบอกว่าเสร็จ**:

- [ ] ถามยืนยัน requirement ก่อนเริ่มแล้วหรือยัง
- [ ] มี spec/task ที่อนุมัติแล้วก่อนลงมือจริงหรือไม่
- [ ] เช็ค root cause / risk / edge case ก่อนแก้แล้วหรือยัง
- [ ] เขียน/รัน testcase แล้วหรือยัง
- [ ] อัปเดตเอกสารที่เกี่ยวข้องครบทุกไฟล์ที่กระทบแล้วหรือยัง (ไม่ใช่แค่ไฟล์เดียว)
- [ ] Archive spec/task ที่ทำเสร็จแล้วหรือยัง
- [ ] Commit + deploy แล้วหรือยัง (ถ้ายัง → **ยังสร้าง Patch Note/Dev Update ไม่ได้**)
- [ ] สร้าง Patch Note + Developer Update แล้วหรือยัง (เฉพาะหลัง commit+deploy สำเร็จ)

ถ้ามีข้อไหนยังไม่ผ่าน ให้กลับไปทำให้ครบก่อน แล้วค่อยรายงานว่าเสร็จ
งานที่ยังไม่ได้ commit+deploy ให้รายงานสถานะว่า "โค้ดพร้อม รอ commit/deploy" แทน ไม่ใช่ "เสร็จแล้ว"
